import 'package:flutter/material.dart';

class MedicsScreen extends StatelessWidget {
  const MedicsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Medics Screen'),
      ),
    );
  }
}