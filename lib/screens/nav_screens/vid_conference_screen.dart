import 'package:flutter/material.dart';

class VidConferencingScreen extends StatelessWidget {
  const VidConferencingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Video conferencing Screen'),
      ),
    );
  }
}
